# convolutional-autoencoder-pytorch
PyTorch implementation of a convolutional autoencoder trained on image datasets for reconstruction tasks, featuring configurable latent dimensions, checkpointing, and visual reconstructions per epoch. Includes support for model resuming, learning rate scheduling, and visualization of input-output image pairs.
